export const domConfig = {
  RENDER_AVG_SAMPLE_SIZE: 100, // How many frames to sample for the average render time
  RESIZE_DEBOUNCE: 100, // Debounce time for resize events (ms)
  STATIC_UID: "Root", // Static UID for the root DOM element. Only used for debugging.
  FRAME_RATE: 60, // Default (target!) frame rate for rendering.
};
